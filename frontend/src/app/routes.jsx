import React from "react";
import Dashboard from "../pages/Dashboard";

// If you add more pages (Login, Settings), add them here
const routes = [{ path: "/", component: <Dashboard /> }];

export default routes;
